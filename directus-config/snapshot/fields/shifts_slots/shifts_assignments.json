{
  "collection": "shifts_slots",
  "field": "shifts_assignments",
  "type": "alias",
  "meta": {
    "collection": "shifts_slots",
    "conditions": null,
    "display": "related-values",
    "display_options": {
      "template": "{{shifts_membership.memberships_user.first_name}} {{shifts_membership.memberships_user.last_name}}: {{shifts_from}} - {{shifts_to}}"
    },
    "field": "shifts_assignments",
    "group": null,
    "hidden": false,
    "interface": "list-o2m",
    "note": "Hier können Mitglieder für diese Schicht eingetragen werden",
    "options": {
      "enableSelect": false,
      "template": "{{shifts_membership.memberships_user}}"
    },
    "readonly": false,
    "required": false,
    "sort": 4,
    "special": [
      "o2m"
    ],
    "translations": [
      {
        "language": "de-DE",
        "translation": "Schichtanmeldungen"
      }
    ],
    "validation": null,
    "validation_message": null,
    "width": "full"
  }
}
